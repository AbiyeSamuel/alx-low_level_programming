#include "main.h"

/**
 * mul - multiplication of 2 numbers
 * @a: first parameter
 * @b: secnd parameter
 * Return: gives the answer of the multiplication
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
